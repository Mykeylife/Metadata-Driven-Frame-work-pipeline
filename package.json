{
  "name": "metadata-driven-framework-pipeline",
  "version": "1.0.0",
  "description": "Repository to hold scaffolding for a metadata-driven ADF pipeline framework",
  "scripts": {
    "adf-build": "download-adf-utilities && validate-adf && compile-adf"
  },
  "devDependencies": {
    "@microsoft/azure-data-factory-utilities": "^1.0.0"
  }
}
