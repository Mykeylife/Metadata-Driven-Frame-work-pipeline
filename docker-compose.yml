version: '3.8'

services:
  orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: metadata_orchestrator_sim
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: python orchestrator.py
